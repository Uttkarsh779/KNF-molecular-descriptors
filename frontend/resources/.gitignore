# These are large build artifacts — never commit them to git.
# They are produced by `npm run electron:dist` before each release build.

# Frozen Python backend (PyInstaller output, ~400-800 MB)
backend/server/

# Bundled external tools (xtb + obabel binaries, ~30 MB)
# Populated by: powershell -File ../backend/NCIForge/scripts/download_tools.ps1
backend/tools/
